{
  "name": "transitgo-functions",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "TransitGo backend, ported to Firebase Functions v2 + Firestore",
  "engines": { "node": "20" },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore,hosting",
    "deploy": "firebase deploy --only functions,firestore,hosting"
  },
  "dependencies": {
    "apns2": "^11.7.0",
    "express": "^4.21.2",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1"
  }
}
